{
  "buildCommand": "cd s117-client && npm install && npm run build",
  "outputDirectory": "s117-client/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
